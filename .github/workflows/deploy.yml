name: 🚀 Deploy to FTP

on:
  push:
    branches:
      - main   # only trigger on pushes to main
  workflow_dispatch:   # 👈 allows manual runs
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📂 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          protocol: ftp
          server: ftp.divader.si
          port: 21
          username: zbarek@pusher.divader.si
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: 	/home/divader/pusher.divader.si/
          local-dir: ./   # upload everything in repo root
